# n8n flows (v1+)

Thư mục này chứa export JSON của n8n workflows, expose qua MCP Server Trigger.

- **v0:** placeholder — pipeline chạy thuần Python (`app/orchestrator/pipeline.py`).
- **v1:** xem issues #9 (first flow), #12 (remaining contracts), #14 (wire pipeline).

`flows/` — export JSON từ n8n UI (trống ở v0).
